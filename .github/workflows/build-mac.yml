# Ce workflow build l'app Electron sur macOS et produit un .dmg
# Il se déclenche quand tu push un tag "v*" (ex: v0.1.0-beta.1)

name: Build Mac

on:
  # Déclenché quand tu push un tag commençant par "v"
  # Ex: git tag v0.1.0-beta.1 && git push origin v0.1.0-beta.1
  push:
    tags:
      - 'v*'

  # Permet aussi de lancer manuellement depuis l'onglet Actions de GitHub
  workflow_dispatch:

jobs:
  build-mac:
    # Utilise un runner macOS hébergé par GitHub (gratuit pour repos publics, 2000 min/mois pour privés)
    runs-on: macos-latest

    steps:
      # 1. Récupère le code source
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Installe Node.js (même version que tu utilises en local)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3. Installe pnpm (version définie dans package.json via packageManager)
      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      # 4. Cache les dépendances pour accélérer les builds suivants
      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      # 5. Installe les dépendances Node.js
      - name: Install dependencies
        run: pnpm install

      # 6. Rebuild better-sqlite3 pour l'architecture Mac (arm64 ou x64)
      # C'est un module natif qui doit être compilé pour chaque plateforme
      - name: Rebuild native modules
        run: pnpm rebuild

      # 7. Installe Chromium via Playwright et le copie pour le bundler
      # Playwright installe Chromium dans ~/Library/Caches/ms-playwright/
      # On le copie dans .playwright-browsers/ pour qu'electron-builder le bundle
      - name: Install Playwright Chromium
        run: npx playwright install chromium

      - name: Copy Chromium for bundling
        run: |
          # Trouve le dossier Chromium installé par Playwright
          PLAYWRIGHT_CACHE="$HOME/Library/Caches/ms-playwright"
          CHROMIUM_FOLDER=$(ls -d "$PLAYWRIGHT_CACHE"/chromium-* 2>/dev/null | head -1)

          if [ -z "$CHROMIUM_FOLDER" ]; then
            echo "❌ Chromium not found in $PLAYWRIGHT_CACHE"
            ls -la "$PLAYWRIGHT_CACHE" || true
            exit 1
          fi

          echo "✅ Found Chromium: $CHROMIUM_FOLDER"

          # Copie dans le dossier qui sera bundlé
          mkdir -p .playwright-browsers
          cp -R "$CHROMIUM_FOLDER" .playwright-browsers/

          echo "📦 Copied to .playwright-browsers/"
          ls -la .playwright-browsers/

      # 8. Debug: Vérifie les credentials Apple (sans révéler les secrets)
      - name: Debug Apple credentials
        run: |
          echo "=== Checking Apple credentials format ==="
          echo "APPLE_ID length: ${#APPLE_ID}"
          echo "APPLE_ID starts with: ${APPLE_ID:0:3}***"
          echo "APPLE_APP_SPECIFIC_PASSWORD length: ${#APPLE_APP_SPECIFIC_PASSWORD}"
          echo "APPLE_APP_SPECIFIC_PASSWORD format check (should be xxxx-xxxx-xxxx-xxxx):"
          if [[ "$APPLE_APP_SPECIFIC_PASSWORD" =~ ^[a-z]{4}-[a-z]{4}-[a-z]{4}-[a-z]{4}$ ]]; then
            echo "✅ Password format looks correct"
          else
            echo "❌ Password format might be wrong. Expected: xxxx-xxxx-xxxx-xxxx (lowercase letters)"
          fi
          echo "APPLE_TEAM_ID length: ${#APPLE_TEAM_ID}"
          echo "APPLE_TEAM_ID: $APPLE_TEAM_ID"

          echo ""
          echo "=== Testing notarytool credentials ==="
          xcrun notarytool history --apple-id "$APPLE_ID" --password "$APPLE_APP_SPECIFIC_PASSWORD" --team-id "$APPLE_TEAM_ID" 2>&1 || echo "❌ Credential test failed"
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      # 9. Build l'app Electron et crée le .dmg
      # electron-vite compile le code TypeScript (et injecte les env vars via define)
      # electron-builder package tout dans un .dmg signé et notarisé
      - name: Build and package
        run: pnpm dist:mac
        env:
          # Signature de code Apple (certificat Developer ID Application)
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          # Notarization Apple (requis pour distribuer hors App Store)
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          # Credentials Google OAuth injectés au build via electron-vite define
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          # Token GitHub pour electron-builder (publish sur GitHub Releases)
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Debug verbose pour electron-builder
          DEBUG: electron-builder,electron-notarize*

      # 9. Upload le .dmg comme "artifact" téléchargeable
      # Tu pourras le récupérer depuis l'onglet Actions de GitHub
      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: Mutuelle-mac
          path: release/*.dmg
          retention-days: 30  # Gardé 30 jours

      # 10. Si c'est un tag, crée aussi une Release GitHub avec le .dmg attaché
      # Tes beta testeurs pourront télécharger depuis la page Releases
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: release/*.dmg
          draft: true  # Créé en brouillon pour que tu puisses vérifier avant de publier
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
