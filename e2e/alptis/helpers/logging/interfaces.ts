/**
 * Logging interfaces for test execution
 *
 * Re-exports from shared logging utilities.
 * Kept for backward compatibility.
 */

export type { ITestLogger, LoggerOptions, LogLevel } from '../../../leads/logging/interfaces';
export { DEFAULT_LOGGER_OPTIONS } from '../../../leads/logging/interfaces';
