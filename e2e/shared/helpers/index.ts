/**
 * Shared helpers for e2e tests
 * These utilities are used across multiple platforms (alptis, swisslifeone, etc.)
 */

export { loadAllLeads } from './loadLeads';
export type { Lead } from './loadLeads';
