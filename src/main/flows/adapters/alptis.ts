export const AlptisAdapter = {
  key: "alptis_sante_select",
  version: "v1",
  requiredFields: [], // TODO
  async execute(_lead: unknown, _creds: { login: string; password: string }) {
    // TODO: playwright steps + mapping
  },
};
